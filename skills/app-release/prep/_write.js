const fs=require('fs');const os=require('os');const p=require('path');const filePath=p.join(os.homedir(),'.claude','skills','store-prep','skill.md');const lines=[];lines.push('---');lines.push('name: store-prep');lines.push('description: Guide for app store submission preparation \u2014 interview, document generation, and technical tasks. Collects app information and prepares all deliverables needed for store registration.');lines.push('user-invocable: true');lines.push('argument-hint: "[phase]"');lines.push('---');lines.push('');fs.writeFileSync(filePath,lines.join('\n'),'utf8');console.log('Phase 1 written');